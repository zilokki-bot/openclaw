# OpenClaw Telegram

Official OpenClaw Telegram channel plugin for Bot API sessions, commands, rich replies,
polling, and delivery runtime support.

The package is published separately from the core OpenClaw package so Telegram channel
runtime fixes can be taken up through the plugin manager without replacing the whole
OpenClaw runtime.
